@using MsSql.AspNet.Identity.Entities

@model List<IdentityDistrict>

<option value="0">Chọn quận/huyện</option>
@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
        <option value="@item.Id">@item.Name</option>
    }
}