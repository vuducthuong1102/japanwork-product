@using ApiMySiteSocial.DB.Sql.Entities
@model List<IdentityLocation>

@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
        <li><a href="javascript:;" data-id="@item.Id" data-long="@item.Longitude", data-lat="@item.Latitude">@item.Name</a></li>       
    }
}