@using MySite.Resources
@using MySite.Helpers
@using MySite.Models

@model List<CsEduHistoryModel>

@if (Model.HasData())
{
    foreach (var item in Model)
    {
        @Html.Partial("Partials/_CsEduHistoryItem", item)
    }
}
