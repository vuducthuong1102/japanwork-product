@using MySite.Helpers
@using MySite.Resources
@model MySite.Models.ApplicationSearchResultModel

@Html.HiddenFor(m=>m.CurrentPage)

<table class="pkges-table mb20">
    <thead>
        <tr>
            <td>@UserWebResource.LB_COMPANY</td>
            <td>@UserWebResource.LB_JOB</td>
            <td>@UserWebResource.LB_APPLIED_DATE</td>
            <td></td>
        </tr>
    </thead>
    <tbody>
        @if (Model.ApplicationList.HasData())
        {
            foreach (var item in Model.ApplicationList)
            {
                if (item.job_info != null)
                {
                    @Html.Partial("Partials/_AppliedJobItem", item)
                }
            }
        }
        else
        {
            <tr>
                <td colspan="20">
                    @UserWebResource.LB_HAS_NO_APPLICATION
                </td>
            </tr>
        }
    </tbody>
</table>

@RenderPaging()

@helper RenderPaging()
{
    if (Model.WasOverRecordsInPage())
    {
        <div class="pagination mb10">
            @Html.BootstrapPager(Model.CurrentPage, index => "javascript:;",
                         Model.TotalCount, pageSize: Model.PageSize, numberOfLinks: 10, onclickEnvent: "ApplicationSearchNext")
        </div>
    }
}
