@using Manager.WebApp.Resources
@using Manager.SharedLibs
@using Manager.WebApp.Models

@model List<JobSeekerCertificateModel>
@{
    var isDetail = false;
    if (Model.HasData())
    {
        isDetail = Model[0].is_detail;
    }
}
<div class="form-group m-form__group row mt10">
    <div class="col-md-12 table-responsive">
        <table class="table table-bordered table-responsive-lg">
            <thead>
                <tr>
                    <th class="text-center" width="50"></th>
                    <th class="text-center">@ManagerResource.LB_CERTIFICATE_NAME</th>
                    <th class="text-center">@ManagerResource.LB_DATE_OF_LICENSE</th>
                    <th class="text-center">@ManagerResource.LB_EDUCATION_STATUS</th>
                    <th class="text-center">@ManagerResource.LB_POINT</th>
                    @if (!isDetail)
                    {
                        <th class="text-center"></th>
                    }
                </tr>
            </thead>
            <tbody>
                @if (Model.HasData())
                {
                    var stt = 0;
                    foreach (var record in Model)
                    {
                        stt++;
                        <tr>
                            <td class="text-center">@stt</td>
                            <td>
                                <b>@record.name</b>
                            </td>
                            <td class="text-center">
                                @record.start_date.DateTimeQuestToString("MM-yyyy")
                            </td>
                            <td class="text-center">
                                @foreach (var myEnum in Enum.GetValues(typeof(EnumCertificateStatus)))
                                {
                                    var chkItem = (int)myEnum;
                                    var chkStr = string.Empty;
                                    if (chkItem == record.pass)
                                    {
                                        chkStr = "selected";
                                        <span>@EnumExtensions.GetEnumDescription((Enum)myEnum)</span>
                                    }
                                }
                            </td>
                            <td class="text-center">@record.point</td>
                            @if (!isDetail)
                            {
                                <td class="text-center">
                                    <a class="m-portlet__nav-link btn m-btn m-btn--hover-info m-btn--icon m-btn--icon-only m-btn--pill" data-modal="" data-href="@Url.Action("UpdateCertificate", "JobSeeker", new { id = record.id, job_seeker_id = record.job_seeker_id })" title="@ManagerResource.LB_EDIT">
                                        <i class="la la-pencil"></i>
                                    </a>
                                    <a class="m-portlet__nav-link btn m-btn m-btn--hover-danger m-btn--icon m-btn--icon-only m-btn--pill" href="javascript:;" data-modal="" data-size="default" data-href="@Url.Action("DeleteCertificate", "JobSeeker", new { id = record.id, job_seeker_id = record.job_seeker_id })" title="@ManagerResource.LB_DELETE">
                                        <i class="la la-trash"></i>
                                    </a>
                                </td>
                            }
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="20">
                            <span class="text-danger"><i class="fa fa-warning"></i> @ManagerResource.LB_NOT_UPDATED</span>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>